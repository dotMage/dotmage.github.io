---
title: dotMage 2.1 — one verb, one command, less to remember
title_ru: dotMage 2.1 — один глагол, одна команда, меньше запоминать
tag: release
summary: dmage sync collapses pull/push into a single verb, dmage open drops you into the admin panel already logged in, shell completions end the flag-guessing, and the admin panel grows real controls. Plus the CI-token and Docker-build fixes.
summary_ru: dmage sync сворачивает pull/push в один глагол, dmage open открывает админку уже залогиненным, автодополнение снимает угадывание флагов, а в админке появились настоящие кнопки. Плюс фиксы CI-токена и docker-сборки.
---

2.1 is a friction release. Nothing about the model changed — still end-to-end encrypted,
still your own tiny server — but the day-to-day got shorter: one verb instead of remembering
pull vs push, one command to open the panel, Tab that actually completes, and an admin panel
you can do things in.

## Highlights

### `dmage sync` — one verb for the 90%

It decides for you: pulls when the server is ahead, pushes your local changes, and if both
moved it shows a key-level diff and asks — no silent merge.

```
$ dmage sync
  ✓ in sync (rev 7).
$ dmage sync            # after editing .env
  local changes → pushing
  ✓ Pushed revision 8.
```

Bare `dmage` in a project now hints when you have unpushed changes.

### `dmage open` — the admin panel, already logged in

```
$ dmage open
  ✓ Opening admin panel — http://your-server:9471
  Logging you in automatically (one-time link, valid 5 min).
```

Finds the panel from `/health` and signs you in with a one-time token carried in the URL
fragment — it never reaches the server's logs.

### `dmage completions` — stop memorizing flags

```
$ dmage completions zsh > ~/.zfunc/_dmage
```

bash / zsh / fish / elvish / powershell. Homebrew installs them automatically.

### An admin panel you can actually use

A Settings page with the server version and an update banner (flip a toggle to follow the
dev channel); team management — change roles, remove members; and **Add a device** — mint a
one-time enrollment link right from the browser. Still metadata-only: the panel never sees
your secret values.

### Fixes worth calling out

`dmage gen-ci-token` was failing with `missing field token_expires_at` — fixed on both
client and server. The server Docker image builds cleanly again.

## Changelog

### Added
- `dmage sync` — pull / push / resolve in one verb; bare `dmage` hints unpushed changes.
- `dmage open` — open the web admin already logged in.
- `dmage completions <shell>` — shell completions to stdout.
- `dmage upgrade --channel dev` — opt into prerelease builds.
- `--json` on `status`, `diff`, `history`, `apps`, `whoami`.
- Admin panel: Settings + update banner + dev-channel toggle, team role/remove, Add-a-device.
- `/health` advertises the web port; tagging a release now publishes a GitHub Release.

### Fixed
- `dmage gen-ci-token` failed with `missing field token_expires_at` — fixed on client and server.
- Server Docker build (`ModuleNotFoundError: No module named 'src'`).
- `dmage env new --copy-from` and `dmage rotate-key` edge cases.

## Downloads

CLI binaries and `SHA256SUMS`: [github.com/dotMage/dotmage/releases/tag/v2.1.0](https://github.com/dotMage/dotmage/releases/tag/v2.1.0)

```
brew upgrade dotmage        # Homebrew
dmage upgrade               # self-update from any direct-binary install
```

Server & web ship as `ghcr.io/dotmage/{server,web}:2.1.0` (and `:2`). Re-run the installer to update:

```
curl -fsSL https://raw.githubusercontent.com/dotMage/server/main/install.sh | bash
```

<!-- ru -->

2.1 — релиз про трение. Модель не изменилась: то же сквозное шифрование, тот же крошечный
сервер — но повседневная работа стала короче: один глагол вместо «pull или push?», одна
команда чтобы открыть панель, работающий Tab и админка, в которой можно что-то делать.

## Главное

### `dmage sync` — один глагол на 90% случаев

Решает сам: делает pull, когда сервер впереди, push — когда есть локальные изменения, а если
разошлось и там и там, показывает key-diff и спрашивает — без молчаливого мерджа.

```
$ dmage sync
  ✓ in sync (rev 7).
$ dmage sync            # после правки .env
  local changes → pushing
  ✓ Pushed revision 8.
```

Голый `dmage` в проекте теперь подсказывает, если есть незапушенные изменения.

### `dmage open` — админка, сразу залогиненным

```
$ dmage open
  ✓ Opening admin panel — http://your-server:9471
  Logging you in automatically (one-time link, valid 5 min).
```

Находит панель из `/health` и логинит одноразовым токеном в URL-фрагменте — он не попадает
в логи сервера.

### `dmage completions` — хватит запоминать флаги

```
$ dmage completions zsh > ~/.zfunc/_dmage
```

bash / zsh / fish / elvish / powershell. Homebrew ставит их автоматически.

### Админка, в которой можно работать

Страница Settings с версией сервера и баннером обновлений (dev-канал включается тумблером);
управление командой — смена ролей, удаление участников; и **Add a device** — одноразовая
ссылка для входа новой машины прямо из браузера. По-прежнему только метаданные: значений
секретов панель не видит.

### Из фиксов

`dmage gen-ci-token` падал с `missing field token_expires_at` — починено на клиенте и сервере.
Docker-образ сервера снова собирается.

## Чейнджлог

### Добавлено
- `dmage sync` — pull / push / resolve одним глаголом; голый `dmage` подсказывает про незапушенное.
- `dmage open` — открыть веб-админку уже залогиненным.
- `dmage completions <shell>` — автодополнение в stdout.
- `dmage upgrade --channel dev` — опт-ин на prerelease-сборки.
- `--json` для `status`, `diff`, `history`, `apps`, `whoami`.
- Админка: Settings + баннер обновлений + dev-тумблер, роли/удаление в команде, Add-a-device.
- `/health` отдаёт веб-порт; тег релиза теперь публикует GitHub Release.

### Исправлено
- `dmage gen-ci-token` падал с `missing field token_expires_at` — починено на клиенте и сервере.
- Сборка Docker-образа сервера (`ModuleNotFoundError: No module named 'src'`).
- Пограничные случаи `dmage env new --copy-from` и `dmage rotate-key`.

## Скачать

Бинарники CLI и `SHA256SUMS`: [github.com/dotMage/dotmage/releases/tag/v2.1.0](https://github.com/dotMage/dotmage/releases/tag/v2.1.0)

```
brew upgrade dotmage        # Homebrew
dmage upgrade               # самообновление для бинарных установок
```

Сервер и веб — `ghcr.io/dotmage/{server,web}:2.1.0` (и `:2`). Чтобы обновить, перезапустите установщик:

```
curl -fsSL https://raw.githubusercontent.com/dotMage/server/main/install.sh | bash
```
